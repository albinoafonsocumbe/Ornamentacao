{
  "name": "backend",
  "version": "1.0.0",
  "description": "API Ornamentação — Plataforma de Decoração de Eventos",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start":       "node server.js",
    "dev":         "nodemon server.js",
    "build:front": "cd ../frontend && npm run build",
    "setup:db":    "node -e \"require('dotenv').config(); const pool = require('./src/config/database'); const fs = require('fs'); const sql = fs.readFileSync('./src/config/setup.sql','utf8'); pool.query(sql).then(()=>{console.log('BD configurada!');pool.end()}).catch(e=>{console.error(e.message);pool.end()})\"",
    "setup:admin": "node criar-admin.js",
    "seed":        "node seed.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "express-validator": "^7.3.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.3.0",
    "nodemailer": "^10.0.10",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
